This directory contains software components that we need to install
manually and where we have specific version requirements (or another
good reason to "cache" them in this repo).

Note: we use generic tarball names so that we do not need to change
scripts once we update some components. These are the currently used
versions:

buildbot-slave		0.8.12
